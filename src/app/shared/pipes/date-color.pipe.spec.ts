import { DateColorPipe } from './date-color.pipe';

describe('DateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
